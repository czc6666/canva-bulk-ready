{
  "name": "canva-bulk-ready",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/checker.mjs && node --check src/app.mjs && node --check src/telemetry.mjs",
    "serve": "python3 -m http.server 4176"
  }
}
